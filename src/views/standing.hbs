<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title></title>
  <link href="/index.css" rel="stylesheet"/>
</head>
<body>
  <div class="olt-Frame">
    <nav role="navigation" class="olt-Navbar olt-Navbar--dark">
      <a href="/" class="olt-Navbar-title"></a>
      <div class="olt-Navbar-content">
        <a href="/" class="olt-Navbar-title"></a>
        <ul class="olt-Navbar-menu olt-Menu">
          <li class="olt-Menu-item">
            <a class="olt-Menu-link" href="/matches/{{tenantId}}">Matches</a>
          </li>
          <li class="olt-Menu-item active">
            <a class="olt-Menu-link is-active" href="/standing/{{tenantId}}">Standing</a>
          </li>
        </ul>
        </div>
    </nav>
    <table class="olt-Table">
      <thead class="olt-Table-body">
        <tr class="olt-Table-row">
          <th class="olt-Table-header">Team</th>
          <th class="olt-Table-header">Points</th>
        </tr>
      </thead>
      <tbody class="olt-Table-body">
        {{#each standing}}
          <tr class="olt-Table-row">
            <td class="olt-Table-data">
              {{players.[0].name}}
              {{#if players.[1]}}
                &amp;
                {{players.[1].name}}
              {{/if}}
            </td>
            <td class="olt-Table-data">{{points}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</body>
</html>
